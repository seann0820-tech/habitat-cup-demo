# 环境、任务与物理边界

本项目使用 Habitat 0.3.3、ReplicaCAD 居家布局和 Fetch 机器人。每个 episode 只完成一个任务；agent 接收任务 ID，导航是共同的前置步骤。环境 schema 为 `home_two_tasks_v2`，障碍生成协议为 `passage_v2`。

## 两项任务

| 任务 | 行为要求 | 成功标准 |
|---|---|---|
| `pick_cup` | 导航到桌边、接近杯子、辅助附着、抬起 | 杯子相对静止位置抬高至少 12 cm，保持 5 个控制步 |
| `open_drawer` | 导航到厨房抽屉、接近把手、辅助附着、拉开 | Habitat 原生滑动关节实际开度至少 18 cm，保持 5 个控制步 |

抽屉使用 ReplicaCAD 自带 `kitchen_counter` 的上层 prismatic joint。单纯到达把手不会自动成功。一个控制步包含多个物理子步；pilot 每个 episode 最多 240 个控制步。指标分别记录任务成功、导航成功、实际步数、碰撞次数和抽屉开度。

Fetch 使用运动学位置控制与阻尼 IK。杯子和把手采用距离门控辅助抓握；抽屉开度由实际末端位移在关节轴上的投影驱动。该设置提供可运行的导航/操作模型学习任务，不代表力控、摩擦抓取或真实手指接触基准。

## A/B/C 条件

- **A 原始环境**：使用原始居家布局。
- **B 通道受阻**：增加一个固定纸箱并更新 NavMesh。原路径受到影响，起点到目标仍连通，增加可测的绕行距离。纸箱也参与底盘几何阻挡。
- **C 操作环境拥挤**：取杯时在桌面增加小纸盒；开抽屉时在把手接近区域侧方增加较高纸箱。不覆盖抓取点，不完全封死导航或达到成功所需的抽屉开度。

B/C 分别相对 A 施加，不累积叠加，也不改变动作增益。新增物体在 episode 内保持静止，均为 Habitat 实际刚体并参与射线与 NavMesh。

新增箱体的机械臂碰撞检查使用多个插值关节姿态上的连杆胶囊、夹持杯子的保守半径，以及抽屉包围盒。检测到碰撞会拒绝对应动作。这是明确的几何近似，不是全机器人连续网格碰撞或完整接触动力学。原有家具接触采用简化控制，不应据此报告真实碰撞安全保证。

## 观测与泛化范围

agent 接收结构化状态、任务/阶段、12 个测距值和新增物体的包围盒，不接收 A/B/C 条件标签。基于可观测几何的绕行/抬手路点由两组共享。

Habitat 全局最短路只用于环境生成、可达性检查和 SPL 统计，不作为策略输入。RGB 只用于录像，因此实验不测视觉识别或视觉域迁移。

训练布局为 `v3_sc0_staging_00`、`v3_sc0_staging_01`、`v3_sc1_staging_00`、`v3_sc1_staging_01`；未见布局为 `v3_sc2_staging_00`、`v3_sc3_staging_00`。seen/unseen 都包含取杯和开抽屉，是同一场景资产体系的布局划分，不是跨房屋类型、机器人或传感器泛化。

## B 条件的确定性生成

箱子半尺寸为 `(0.16, 0.27, 0.16)` 米。候选位置包括沿无障碍最短路径的位置及左右偏移。起终点需留空，加入障碍后的路径仍连通，并比原最短路径长超过 0.03 米。

若原始起点路径无法满足条件，在同一场景和同一目标下最多尝试 8 个起点；无障碍测地距离保持在原 1.5–4 米范围内，保留原朝向。最多尝试 264 个箱子放置候选。过程只查询几何，不调用 agent、不缩小箱子、不取消障碍、不放宽成功标准。

这得到按几何可行性条件化的 B 分布。发生起点重采样时，同 seed 的 A/B 不能解释为“初始条件完全相同、只多一个箱子”；同一 B 条件下 baseline 与元认知仍必须匹配相同几何。JSON 记录原/新起点、候选次数、障碍前后路径长度及其差值。

如果全部候选失败，程序明确停止并写入场景、任务、seed 和拒绝统计。该情况不算 agent 失败，也不会静默删除再改变成功率分母。compare 核对两组的起点、目标和障碍布局，失败的策略轨迹同样需要匹配。

## 几何预检与参考控制器检查

Cell 4 的参考检查在一个固定布局/seed 上执行两任务 × A/B/C，使用未训练的先验 MPC。A 失败时停止以排查基础接口；B/C 的策略失败保留。该检查不进入训练 replay，不是训练模型的零样本评估，也不是成功率估计。

Cell 7 的预检覆盖指定持续学习协议使用的全部不同初始环境。默认每个训练 seed 共 240 次 reset，覆盖训练、验证和重复使用的测试初始条件；不执行动作或学习。通过后可按协议缓存，正式实验依然用相同种子重建环境。

预检时间单独保存，不能隐含为 agent 的免费计算。几何可行不等于策略一定成功，也不构成全局机械臂可达性证明。定位失败时应查看导航是否完成、动作拒绝/碰撞、阶段切换和实际目标进展；不应直接提高时间上限或放宽成功标准掩盖问题。

持续学习结果目录包含 `passage_v2` 标识。更改几何分布或成功标准后，需要新的协议标识与独立实验目录，避免混合不可比较的结果。

## 资料

- [Habitat-Sim 0.3.3 源码](https://github.com/facebookresearch/habitat-sim/tree/v0.3.3)
- [ReplicaCAD 数据集](https://aihabitat.org/datasets/replica_cad/)
- [Habitat articulated object API](https://aihabitat.org/docs/habitat-sim/habitat_sim.physics.ManagedArticulatedObject.html)
