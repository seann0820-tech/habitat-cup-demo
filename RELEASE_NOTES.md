# 变更记录

## 2026-09-10

- 将运行入口与已有实验记录分开保存；入口保留两个种子和 Drive 默认设置。
- 修复关闭录像时仍访问 `SEEDS` / `OUTPUT_ROOT` 的错误，并为重连后的录像增加配置检查。
- 添加源码同步工具，使独立 Python 文件的改动可以写回 Colab notebook。
- 收录汇总结果、阶段曲线、任务视频和研究报告。
- 精简仓库首页，补充设计取舍、代码阅读顺序和测试目的。

原有 25 个 Python 文件与最终实验 notebook 内嵌版本一致。文件摘要和运行配置记录在 `RELEASE_MANIFEST.json`；原始运行 notebook 保留在 `results/`。
