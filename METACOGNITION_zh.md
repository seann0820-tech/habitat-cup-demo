# 元认知模块：操作定义、监督与解释边界

本文件解释 `meta_comparison_v2` 中“元认知”具体指什么，帮助区分监测能力、控制能力和最终任务表现。任务目标是比较固定预算 MPC 与一套可学习的计算调控器；不会预设元认知必然提高成功率或降低计算量。

## 文献怎样影响设计

| 文献 | 与本项目相关的观点 | 本项目的工程选择 |
|---|---|---|
| [Guggenmos, 2022, Reverse engineering of metacognition](https://elifesciences.org/articles/75420) | 区分一级决策、元认知读出、信心转换/报告及其噪声和偏差 | 将监测分数、概率校准和控制价值分开记录与验证 |
| [Fleming & Daw, 2017](https://pubmed.ncbi.nlm.nih.gov/28004960/) | 元认知可视为对自身决策过程的二阶推断 | 读取世界模型预测、规划搜索和实际执行证据，预测自身过程可靠性 |
| [Desender, Boldt & Yeung, 2018](https://pubmed.ncbi.nlm.nih.gov/29608411/) | 信心与继续搜集信息的选择有关 | 允许监测影响资源分配，但不把低信心等同于必须多算 |
| [Hamrick et al., 2017](https://arxiv.org/abs/1705.02670) | 学习如何分配内部模拟资源 | 按批追加候选轨迹，学习追加计算是否值得，计算成本显式进入目标 |
| [van den Berg et al., 2016](https://elifesciences.org/articles/12192) | 初始决定后积累的证据与改变决定、信心有关 | 累积真实执行后的误差/碰撞/进展，作为下一次调控依据 |

这些文献提供可检验的设计动机，不证明本实现与人的机制相同。这里不是 ReMeta 的参数拟合：机器人动作不是标准二分类判断，确定性网络误差也不能直接解释为人类模型的元认知噪声。没有人为注入“人类式噪声”，没有把任务代价改名为自由能。

## 一级控制器与观测

世界模型是三个 38→64→64→6 MLP 的集成，输入包含结构化状态、动作、任务阶段及几何信息，预测真实动作后的状态变化。baseline 用固定 CEM 搜索有限时域的动作序列，每步执行当前最好序列的第一个动作。

两组拥有相同的任务成功标准、动作空间、阶段限制、成本函数、几何引导和辅助抓握。元认知没有新的抓取技能、全局最短路信息或 A/B/C 条件标签。它能改变何时停止搜索、追加多少搜索、从何种分布搜索或回退到原固定 MPC。

“相同世界模型”指结构、初始化种子和学习规则一致。两组独立采集训练轨迹、保存 replay 和更新权重，训练后的动力学权重不保证相同。主要比较反映整个学习与控制系统的表现，不能将全部收益单独归因于监测。

RGB 用于录像，不作为控制器输入。当前世界模型更新日程和 FIFO replay 在两组之间相同；这些不是元认知控制动作。

## 监测证据与诊断预测

监测器使用 28→32→32→7 的共享 NumPy MLP，七个输出对应三个诊断量和四个调控价值。从真实可用的内部证据中提取表示，并分别输出：

- 当前任务阶段在预算内完成的预测分数与概率；
- 实际下一动作发生碰撞/被拒绝的概率；
- 实际一步世界模型预测误差的期望值；
- 不同计算调控选项的价值。

输入包含任务/阶段、剩余预算、模型分歧、候选改善、动作变化、近期预测误差、碰撞和执行进展。模型分歧表示多个动力学模型的不同预测，不直接等于已校准信心。

执行后的证据先与此前付费的一步预测比较，再更新 episode 局部历史。局部历史在每个新 episode 重置；不能读取尚未发生的真实转移。

**进展不是到最终目标的直线距离。**绕障可能暂时远离目标。停滞判断应结合实际位移、重复位置、碰撞与当前路点的进展，并对阶段/路点切换作明确处理。监测器据此估计不可靠性，不将一次误差自动解释为特定失败原因。

## 分数与概率校准

概率预测区分未经校准的原始分数与校准后的输出。低样本下可能出现两类不同问题：分数不能区分成功/失败，或分数排序有用但概率系统性偏高/偏低。校准只能帮助第二类问题，无法凭空增加证据分辨能力。

每第五个非先验、产生元记录的训练 episode 整体留作校准，不写入共享网络/Q 的训练集合。该集合不用于共享监测网络的普通拟合；正式验证/测试及共同轨迹诊断不参与拟合或调参。整个 episode 的记录必须分到同一侧，不能把高度相关的相邻步骤随机拆成训练与校准数据。

校准分别对阶段完成和碰撞头使用全局 logit 仿射变换 `sigmoid(exp(log_scale) × logit + bias)`，不按 A/B/C 分组。每个头至少需要 32 条可用记录、3 个独立保留 episode 以及两类结果，才启用校准。拟合按 episode 等权，并向恒等变换正则化；保留不足时输出未经校准的概率。校准参数优化上限为 80 次标量更新，学习率 0.05，正则项 0.02；`log_scale` 限于 [-2,2]，偏置限于 [-3,3]。

校准器及其支持样本量一起保存在 checkpoint 中。共享控制器就绪要求至少 4 个共享训练 episode、每个调控选项至少 4 条存储记录，以及至少一次网络更新。这些都是覆盖门槛，不是准确率或校准质量保证。校准缓冲区上的拟合分数也不是独立测试成绩。

## 计算控制

每个动作先进行有界的小批搜索，再根据共享过程证据预测的 Q 值选择以下选项：

| 选项 | 操作含义 | 计费原则 |
|---|---|---|
| `EXECUTE` | 停止内部搜索，执行当前最好动作 | 已完成的搜索和监测仍计费 |
| `EXPAND` | 追加一小批候选，随后重新判断 | 每一批模型预测和监测都计费 |
| `RESTART` | 改变搜索初始化后追加一批候选 | 已花计算不退回，也不访问真实未来状态 |
| `BASELINE` | 调用原来的完整固定预算 MPC | 前缀、监测以及回退 MPC 全部计费 |

阶段/碰撞概率和误差预测是诊断辅助头；Q 头直接决定调控。辅助任务可以改变共享表示，但校准后的概率不会直接替代 Q，也不会通过阈值自动决定执行/追加搜索。因此仅改善校准并不保证行为改变。

这不是“信心大于 85% 就执行”的规则。N85 的 85% 是任务学习曲线的评估门槛，不是元认知的单步控制阈值。

控制器学习的是实际选择后的任务收益与计算成本。固定搜索长度、网络规模和候选数量决定的即时 FLOPs 可直接计算；无需让网络猜测这部分成本。额外计算是否改善真实任务仍需通过实际反馈学习，不能用世界模型自己的代价下降证明自己有效。

调控记录只给真正执行的选项赋值；未执行选项没有反事实成功标签。Q 目标为实际即时收益加上折扣后的目标网络下一允许动作最大值。目标网络在每次 SGD 后以 0.02 的 Polyak 系数更新；只监督选中动作的 Q，不给其他动作伪造标签。时序差分目标缩短长期成功标签的信用分配距离，但仍受策略变化、世界模型变化和有限探索影响。训练保留调控探索，测试冻结参数和训练随机状态。

## 局部监督与训练记录

阶段完成、碰撞和一步预测误差使用有明确时间对应的真实训练标签。最终任务成功仍是行为收益的一部分；阶段完成辅助监督不会把“导航成功”替代为完整取杯/开抽屉成功。

阶段完成标签要求相应完成事件发生在预测之后。导航到接近、接近到抓握是向前的完成事件；最后的抬升/拉动阶段必须达到任务成功标准。抽屉手柄脱落导致的阶段退回不是完成；同一阶段反复抓握也不重复获得里程碑奖励。

元认知最多保留 512 条普通训练记录和 128 条校准记录，每个 episode 有界抽取至多 16 条相关记录。共享网络默认每次拟合 24 次更新、batch 64、学习率 0.01，采用梯度裁剪 SGD。每条记录不是独立 episode，不能计入环境样本数量。世界模型的 4096 条 transition FIFO 与这些元记录分开计数。

世界模型和元模型参数、校准参数、缓冲区与相关随机状态一起保存/恢复。验证、测试及共同轨迹诊断仅更新 episode 局部推理历史，不写训练记录、不改变权重、不重新校准。

## 怎样检验监测与控制

闭环成功率只能说明整个系统的表现。它同时受世界模型、规划能力、监测和控制影响，不能单独归因于元认知读出。

共同轨迹诊断让固定 baseline 世界模型/规划器执行，冻结的元监测器只读取相同轨迹上的证据并预测，不改变动作、不学习。诊断上的原始/校准预测可以判断概率转换是否有用；正式闭环比较再判断调控是否改善表现与成本。这个诊断存在从 baseline 轨迹到元策略轨迹的分布差异，不作为元策略反事实收益的证据。

观察者使用 baseline 世界模型的独立冻结副本计算搜索证据，元监测器来自元认知组的基础训练 checkpoint。这保证两种概率转换看到完全相同的证据，但也引入世界模型证据分布的变化，应与元认知自身闭环轨迹上的诊断一起阅读。

建议按任务/阶段报告预测样本数、类别分布、Brier/ECE、碰撞预测与误差预测；高度相关的逐步记录不能当成独立 episode 做置信区间。没有成功/失败两类时，某些区分度指标不能计算，应保留缺失而非填零。

共同轨迹的额外推理、世界模型预测和环境步数单独保存，不算训练样本，也不隐含为免费的验证。基于正式测试选择成本权重、校准参数或监测结构会污染测试，应在独立训练内部数据上完成选择。

## 计算与内存的边界

计数包括搜索前缀、每次追加/重启、固定 MPC 回退、实际动作的一步预测、元网络推理、世界模型更新、元网络更新与校准拟合的已实现估计。模型前向的 FLOPs 是工程近似，更新按实现的估计规则记录。

该计数不是物理电耗，未包含 Habitat 物理、渲染、IK、全部几何/CEM数组运算、数据传输与运行时开销。批量前向次数、样本前向次数、环境步数、壁钟时间是不同指标，不能互相替代。数组内存估计不含 Python 对象管理开销。

## 暂未实现的扩展

- 主动感知与部分可观测信念模型；
- 自适应世界模型更新频率、replay 选择或容量分配；
- 新的操作恢复技能或更长时域规划器；
- 人类 ReMeta 参数恢复、独立 meta-d′/meta-noise 实验；
- active inference 的明确生成模型与 expected free energy 控制。

学习/replay 调度可借鉴 [Meyniel & Dehaene, 2017](https://pubmed.ncbi.nlm.nih.gov/28439014/) 对可靠性与学习的讨论，以及 [Mattar & Daw, 2018](https://www.nature.com/articles/s41593-018-0232-z) 的 gain/need 思路。它们属于后续假设，不能从本版结果宣称已经得到验证。
